export enum PfDataGridColType {
  default = 'default',
  dateTime = 'dateTime',
  currency = 'currency',
  pricingInfo = 'pricing-info',
  template = 'template',
  sharedTemplate = 'sharedTemplate',
  bit = 'bit'
}
